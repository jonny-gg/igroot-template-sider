/**
 * 获取列表
 * @param {入参} value 
 */
export const getProjectsApi = (value = {}) => {
  return Client.query(`{
    
  }`)
}

/**
 * 新增
 * @param {入参} value 
 */
export const addProjectApi = (value = {}) => {
  return Client.mutate(`{
    
  }`)
}

/**
 * 删除
 * @param {入参} value 
 */
export const deleteProjectApi = (value = {}) => {
  return Client.mutate(`{
    
  }`)
}


/**
 * 更新
 * @param {入参} value 
 */
export const updateProjectApi = (value = {}) => {
  return Client.mutate(`{
    
  }`)
}